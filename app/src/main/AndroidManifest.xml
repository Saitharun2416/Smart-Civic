<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCivicGovernance"
        android:windowSoftInputMode="adjustResize">

        <!-- Google Maps API Key Placeholder -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_MAPS_API_KEY_HERE" />

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartCivicGovernance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Flow -->
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="false" />

        <!-- Citizen Flow -->
        <activity
            android:name=".ui.citizen.CitizenDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.complaint.ReportComplaintActivity"
            android:exported="false"
            android:label="Report Complaint" />
        <activity
            android:name=".ui.complaint.ComplaintDetailActivity"
            android:exported="false"
            android:label="Complaint Details" />
        <activity
            android:name=".ui.complaint.MapViewActivity"
            android:exported="false"
            android:label="Map View" />

        <!-- Worker Flow -->
        <activity
            android:name=".ui.worker.WorkerDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.worker.TaskDetailActivity"
            android:exported="false"
            android:label="Task Details" />
        <activity
            android:name=".ui.worker.SubmitProofActivity"
            android:exported="false"
            android:label="Submit Proof" />

        <!-- Admin Flow -->
        <activity
            android:name=".ui.admin.AdminDashboardActivity"
            android:exported="false" />

        <!-- File Provider for Camera Photo Saving -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.smartcivicgovernance.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
